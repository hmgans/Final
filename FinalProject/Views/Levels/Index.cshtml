@model IEnumerable<FinalProject.Models.Level>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BackGroundImage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LevelObsticles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BackGroundImage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LevelObsticles)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LevelID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LevelID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LevelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
