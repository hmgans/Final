@model IEnumerable<FinalProject.Models.Bot>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LevelID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Recording)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.time)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LevelID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recording)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.time)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BotID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BotID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BotID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
